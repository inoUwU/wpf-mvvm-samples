<Window x:Class="wpf_mvvm_sample.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:wpf_mvvm_sample"
        xmlns:viewModels="clr-namespace:wpf_mvvm_sample.ViewModels"
        xmlns:ct="clr-namespace:wpf_mvvm_sample.CustomControls"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.DataContext>
        <viewModels:MainWindowVm />
    </Window.DataContext>
    <Window.Resources>
         <CollectionViewSource
                    x:Key="SexTypeComboBoxItemSource"
                    Source="{Binding SexTypeComboBoxItems}" />
    </Window.Resources>
    <DockPanel Margin="10">
        <StackPanel DockPanel.Dock="Top" HorizontalAlignment="Left">
            <Button Content="SendMessage" Command="{Binding SetFocusCommand}" Margin="0 10 0 0" />
            <ct:YearMonthPicker Width="Auto"
                                SelectedDate="{Binding SelectedYearMonth, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                Margin="0 10 0 10">
            </ct:YearMonthPicker>
        </StackPanel>

        <DataGrid x:Name="MainDataGrid" ItemsSource="{Binding People}" AutoGenerateColumns="False" Margin="10"
                  CanUserAddRows="True"
                  LoadingRow="DataGrid_LoadingRow"
                  >
            <DataGrid.Resources>
                <Style TargetType="DataGridCell">
                    <Setter Property="Validation.ErrorTemplate">
                        <Setter.Value>
                            <ControlTemplate>
                                <DockPanel LastChildFill="True">
                                    <Border BorderBrush="Red" BorderThickness="2">
                                        <AdornedElementPlaceholder />
                                    </Border>
                                </DockPanel>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <Trigger Property="Validation.HasError" Value="True">
                            <Setter Property="ToolTip"
                                    Value="{Binding RelativeSource={RelativeSource Self}, Path=(Validation.Errors)[0].ErrorContent}" />
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.Resources>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name"
                                    Binding="{Binding Name, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"
                                    IsReadOnly="False"/>
                <DataGridTextColumn Header="Age"
                                    Binding="{Binding Age, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"
                                    IsReadOnly="False"/>
                <DataGridComboBoxColumn Header="Sex"
                                        ItemsSource="{Binding Source={StaticResource SexTypeComboBoxItemSource}}"
                                        SelectedValuePath="Value"
                                        DisplayMemberPath="Name"
                                        SelectedValueBinding="{Binding Sex, UpdateSourceTrigger=PropertyChanged}"
                                        IsReadOnly="False"/>
            </DataGrid.Columns>
            <DataGrid.CellStyle>
                <Style TargetType="DataGridCell">
                    <EventSetter Event="GotFocus" Handler="DataGridCell_GotFocus" />
                </Style>
            </DataGrid.CellStyle>
        </DataGrid>
    </DockPanel>
</Window>